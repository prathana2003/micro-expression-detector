[project]
name = "micro-expression-detector"
version = "0.1.0"
description = "Real-time micro-expression and facial signal detection system"
requires-python = ">=3.11,<3.13"

[tool.vercel]
entrypoint = "backend.app.main:app"